# ROADMAP — 移植版與設計稿的差距

第一階段（五個 change）交付的是**設計稿的主要功能**，不是設計稿的全部。這份檔案是那個差額的
單一清單，讓後續工作不必每次重讀 `design/HANDOFF.md` 才知道還缺什麼。

第六個 change（`bound-learnset-scroll`）已 archive —— 它不是補這份清單裡的缺口，而是推翻了
C 節的一項決定，見「曾列在 C 節、已被推翻」。

第七個 change（`surface-dataset-facts`）已 archive：A7 全數交付並移出本清單；A2 只交付版權那一段，
五段來源說明已決定永久不做並移至 C 節。行為寫在 `openspec/specs/dataset-statements/`。

**這份檔案管什麼**：設計稿有、移植版沒有的東西，以及移植版整體缺的非功能能力。
**這份檔案不管什麼**：平台事實（`design/HANDOFF.md` §12）、資料來源與推導規則（同檔 §5–§7）、
已完成功能的行為（`openspec/specs/`）。三者都還是各自的權威，這裡只在需要時指路。

最後對照：2026-08-09，對照的是 `widen-dex-query-filters` archive 之後的樹。

**A 節現為兩項：A6、A10。** 五項在 2026-08-08 至 08-11 之間結清：

| 項目 | 結果 | 行為寫在 |
|---|---|---|
| A3 `prefers-reduced-motion` | 實機量測後判定不做，移入 C 節 | §12.23 |
| A8 卡片 BST 數字 | 已交付（`surface-bst-on-cards`） | `openspec/specs/species-card/` |
| A4 屬性多選 | 已交付（`widen-dex-query-filters`），多值為 OR | `openspec/specs/dex-query/` |
| A5 兩顆篩選鈕 | 同上，查詢列因此增為三列 | 同上 |
| A11 招式 flag 的顯示範圍 | 已交付（`surface-move-flags`），17 種簡稱 | `openspec/specs/move-detail/`、`openspec/specs/dex-data/` |

前一次對照（2026-08-08，`optimize-query-bar` 之後，`c617a76`）：當時的 A 節六項與 B 節六項逐項
重驗全部仍然成立。B 節六項至今仍未動過，**但其中「自動化測試」一列已因本次發現而擴大** ——
見該列。

> 更正：2026-07-30 到 2026-08-08 之間，這裡寫著「A 節現為七項：A3–A6、A8–A10」。
> **A9 從來不存在**，實際是六項。編號不重編的規則本來就會產生缺號（見「維護方式」），
> 而那次把缺號當成了實有項目。數項目請數 `### A` 標題，不要數區間端點。

> 更正：這一行在 2026-07-30 到 2026-08-08 之間寫著「A 節現為七項：A3–A6、A8–A10」。
> **A9 從來不存在**，實際是六項。編號不重編的規則本來就會產生缺號（見「維護方式」），
> 而那次把缺號當成了實有項目。數項目請數 `### A` 標題，不要數區間端點。

其後兩個 change 都**不是**補這份清單的缺口：`add-move-learners` 是設計稿沒有的新功能，
`optimize-query-bar` 的影響已寫進 A4 的註、A6 的排序控制項段落與 B 節無障礙欄。

前一次對照（2026-07-30，`surface-dataset-facts` 之後）的 A 節變動：A1 氛圍層與 A2 五段來源說明
都決定不做並移至 C 節（§12.20、§12.21）；A7 tally 已交付並移出。

---

## 為什麼這些項目會漏掉

值得先講，因為它決定了怎麼避免下一批再漏。

移植版的 spec 是**逐批**寫的，每一批只描述那一批要交付的東西。設計稿裡沒被排進任何一批的
東西，因此既不在 spec 裡（所以 `spectra` 的檢查看不到），也不在 §12.16 的偏離清單裡（那份清單
只涵蓋詳情面板與招式表），也不在 `pnpm run check` 裡。**缺的東西從來沒有被要求過。**

這與 §12.18 記的失效是同一個機制的另一半：那次是字串跑在實作前面（佔位字承諾了未實作的搜尋
範圍），這次是字串跟著實作一起消失 —— `i18n.ts` 少了 `footer`、`megaOnly`、`multiOnly`、
`sortName`、`count` 與四個 tally 鍵，於是畫面上沒有任何東西指出功能不見了。**§12.18 只寫了
前一半。**

`surface-dataset-facts` 補回了 `footer`、`count` 與四個 tally 鍵；`widen-dex-query-filters` 補回
`megaOnly` 與 `multiOnly`。**只剩 `sortName`** —— 這個鍵是否存在，仍然是 A6 有沒有被做的最快指標。

---

## A. 功能差距

「文件已經預告、但沒有交付」的項目原本有三項，那比單純的少做一個功能更貴，會讓後人相信它
已經存在。**三項現在都結清了**：A1 氛圍層（§12.20）、A2 五段來源說明（§12.21）與 A3
`prefers-reduced-motion`（§12.23）都已判定不做並移至 C 節。剩下的 A 節兩項都沒有被設計稿預告過
—— A6 與 A10 都是設計稿有而移植版沒做的。

A11（招式 flag 的顯示範圍）是唯一一項**不是**這個形狀的：資料是移植版自己新增的，取捨在
2026-08-11 定案並由 `surface-move-flags` 交付。它已移出本節，行為寫在 `openspec/specs/move-detail/`
與 `openspec/specs/dex-data/`。**編號不重編**，所以 A 節從此是 A6、A10 兩項而中間缺 A11。

### A6. 名稱排序

設計稿三種排序（編號／種族值／名稱），移植版兩種。

**平台問題已於 2026-08-08 實機量測結清，而且結論是好消息（§12.23）。** `Intl` 在這個平台上是
`undefined`，`localeCompare` 就是 code point 相減、locale 引數被無視 —— 但兩個語系都不需要 ICU
就能得到正當的排序：英文名全是「首字大寫其餘小寫」的純 ASCII，逐筆比對過 code point 排序與
不分大小寫字母排序**完全相同**；中文的 code point 序即部首序，排序結尾是龍部、龜部，與康熙部首表
一致。所以**用最樸素的 `<` 比較即可，不要引入 `localeCompare`，也不要動 pipeline 加排序鍵**。

若日後 `Intl` 出現，這兩個排序都不必改 —— 它們的正確性不依賴 ICU 缺席。屆時要不要換成拼音序
是一個新決定（部首序 vs 拼音序），不是修 bug。

`dex-query` spec 用「closed set containing **at least** national number and base-stat total」
把兩種排序合法化了，補第三種不違反既有 spec。

**但排序控制項要一起改。** `optimize-query-bar` 之後排序是單顆循環晶片（按一下推進到集合的
下一個成員），這在二元集合上成立、在三元上會開始難用：要按兩下才到得了最後一項，而且畫面上
看不出總共有幾個選項。要補第三種排序，就要把排序控制項改成可展開的選單 —— 那會佔回一部分
`optimize-query-bar` 省下的垂直空間，兩件事要一起評估，不能只加成員。

### A10. 離線探測與整批佔位

設計稿開機時探測一次遠端圖庫（`probe()` / `REMOTE`），失敗就**整批**改用繪製的佔位圖，理由
寫在原始碼註解裡：避免數百張破圖閃過。移植版是逐張 `@load`，離線時每張卡各自等自己的 timeout。

§12.16 只記了移除 `new Image()` 預熱，沒記整批探測這件事。要不要補取決於離線是否算目標情境 ——
若不算，把這個決定寫下來就夠了。

---

## B. 非功能差距

這些不是「設計稿有而移植沒有」，而是移植版整體缺的能力。`HANDOFF.md` 沒有涵蓋任何一項。

| 項目 | 現況 | 影響 |
|---|---|---|
| **自動化測試** | **已建置（2026-08-09）**，`vitest` + `tests/`，66 個測試 | `dex-query` 與 `dex-data` 的 Example 表已全數機器驗，且測的是真模組（`vue-lynx` 的反應式在 node 下可用，`query.ts` 直接被驅動）。**仍未涵蓋**：`learnset-table` 的六欄左緣 195/223/267/311 那類版面數字（需要量測而非邏輯）、`species-card`／`species-detail`／`move-learners` 的 Example、以及所有需要元素樹的東西。下一批要補就從 `learnset` 與 `moveLearners` 兩個 state 模組開始 —— 它們與 `query.ts` 同構，同樣可直接驅動 |
| **`.vue` 型別檢查** | **已補（2026-08-09）**，`vue-tsc` + `pnpm run typecheck` | 診斷與補救都經實測（種入型別錯誤：`tsc` exit 0、`vue-tsc` exit 2）。**殘留缺口**：`vue-lynx` 的 Volar plugin 是 API v1、現行 vue-tsc 要 2.x，所以載入時被拒，**Lynx 元素（`<view>`／`<text>`／`<image>`）的屬性仍然不受檢查** —— 假屬性照樣通過。鑑於 §12 反覆記載的失效正是「屬性看起來宣告了卻沒作用」，這一塊仍只能靠實機。要真正補上得等 vue-lynx 更新 plugin API |
| **CI** | 沒有 `.github/` | 四項 check 靠人記得跑 |
| **無障礙** | 移植版零 a11y 屬性 | 設計稿有 `aria-pressed`（模式／語系／屬性／排序鈕）、`aria-current`（選中的卡片）、`role="dialog"` + `aria-modal`、`label for`。Lynx 的 accessibility 屬性**沒查過**。這個主題 HANDOFF 全文未提。**`optimize-query-bar` 之後多了一筆**：搜尋輸入框不再有可見標籤，它的身分現在只由 placeholder 承載 —— 處理這個主題時它需要一個 accessibilityLabel，而不是靠 placeholder 兼任 |
| **效能與記憶體數字** | **延遲已定因並已改善（2026-08-10）**，記憶體仍沒有 | **成因：平台按元素計價，約 1.3ms 一個**（§12.24）。五刀單變數對照逐一排除網路、圖片快取、屬性字符、文字塑形、主線程觸控綁定——與那些元素是什麼無關，就是建立元素本身。**處置：三處長序列改為自管的可視範圍視窗**（`window-visible-range`），只渲染可見區間加半屏緩衝。前後數字：詳情面板 #475 艾路雷朵 **897ms → 285ms**；未篩選網格最慢 sprite **2327ms → 約 200ms**，靜止時渲染 16 張卡而非 208 張；學習者清單（最多 225 列）改前未量，改後目視無空白無錯配。⚠️ 這一列曾寫「字符光柵化 ~7.5ms／元素」，**數量級對但單位與歸因都錯（是每列不是每字符），已作廢**。**記憶體仍然零數字** |
| **Android** | 兩筆掛帳（§10：字符渲染、字型註冊）| **矛盾已於 2026-08-08 結清：定調為不在範圍內。** §12.17 末尾是權威，§10 的阻擋性標題已移除並指回該處。兩筆保留為「日後納入範圍時要驗的事」。**這一列留在 B 節，因為掛帳本身還在** —— 但它不再擋任何工作 |
| **驗收截圖** | `shots/` 停在 13 張 | §12.17 兩項與 §12.19 三項 iOS 結清項目都沒有截圖（兩節都自己承認）。重跑回歸的條件各寫在該節末尾。**手勢類的驗收本來就拍不出來** —— §12.19 那三項是「哪一層吃到手勢」，靜態截圖無法呈現，要留證據得錄影 |

---

## C. 已確認不做 —— 不要當成待辦重新提出

每一項都有平台或設計理由，理由在指定的位置。**在改回設計稿的寫法之前先讀它。**

| 設計稿的東西 | 為什麼不做 | 理由在 |
|---|---|---|
| 四處 `title` hover 提示（屬性中文名、形態鈕屬性、招式另一語言名、傷害類別全名） | 觸控裝置沒有 hover。前兩者的資訊已直接寫進藥丸與鈕，後兩者不需要替代物 | §12.16、§8 末列 |
| 左右方向鍵切換形態、Escape 關閉面板 | 裝置沒有鍵盤（§3 的功能敘述尚未更新，是文件債不是功能債） | 本檔 |
| `<list>` 承載網格或招式表 | vue-lynx 只實作尾端追加，篩選與排序在它上面不成立。**這是「當時不能用」不是「本專案偏好 scroll-view」** | §12.13（含重新評估的觸發條件） |
| 把屬性字符從 `<svg content>` 改成 `<image>` **以改善效能** | **已實作到一半、實機量到零改善後整個撤回（2026-08-10，change `cache-glyph-rasterisation`）。** 前提是「字符光柵化是詳情面板等待的原因」，而那是共變污染的錯誤歸因：拿掉招式列全部 105 個字符元素後，#475 艾路雷朵仍是 903ms（原 897ms）。**字符不是成本。** 附帶量到而仍然成立的三件事留在 §12.24：PNG data URI 在 iOS 畫得出來且最近鄰有效、相同 URL 會被 URL 鍵快取收斂 16 倍、以及 SVG 那條路確實無快取又走序列佇列（但**從未被單獨量過多貴**）。**若日後有人為了別的理由（不是效能）想換基元，上面那三件事可以直接引用；但不要再用效能當理由，除非先有一個只改變字符、其他都不動的量測** | §12.24（開頭的更正框）|
| `font-variant-numeric: tabular-nums` | 平台的字型註冊不吃 font-variant 描述子，且像素字型的數字本來就不等寬。**樣式表不得出現這個屬性** | §12.17 |
| `new Image()` 預熱其他形態的圖 | 平台沒有 `Image` 建構子 | §12.16 |
| 三處 `display: grid` | `App.css` 零處 grid，flex + 固定欄寬對齊結果相同 | §12.16 |
| 半透明遮罩以外的任何半透明表面 | 會合成出四階灰以外的顏色。遮罩是唯一例外，**不是前例** | §12.16、`retro-theme` spec |
| 存取捲動位置的程式 | Vue 的響應式更新本來就留著捲動位置。**不要「補」上去** | §12.16 |
| 面板**其他區段**自帶捲動區 | `species-detail` spec 把例外限給招式表，並明文寫「不是前例」。⚠️ 這條的理由已經換過：§12.19 實測巢狀同向捲動**技術上成立**，所以「辦不到」不再是理由，剩下的是「沒有一段長到需要」—— 種族值六列、特性最多三塊、大圖固定 192px。**拿 §12.19 當通行證在其他區段加捲動區是誤讀** | §12.19、`species-detail` spec |
| 搜尋語料收錄裸羅馬數字世代 token | 搜 `V` 會命中 208 筆裡的 125 筆，與「搜尋壞了」無法區分 | §12.18 |
| footer 的五段來源說明（陣容與形態／招式／種族值與特性／中文名稱／圖像） | 討論後決定永久不做。**代價是移植版畫面上沒有任何資料來源聲明**，`README.md` 有但使用者看不到；且 §8「第六世代之後是渲染圖」的現況處理在移植版不成立（§8 已標注）。`dataset-statements` spec 明文要求 footer **不得宣稱資料來源**並附 scenario，使「沒有來源說明」成為被要求的狀態而非沒人發現的缺口。字型／版權那一段**已交付** | §12.21（含三條重新評估的觸發條件） |
| `prefers-reduced-motion` 關閉四個動畫（原 A3） | **不是「平台不支援這個 media feature」，是整個 `@media` 到不了平台** —— CSS 管線在建置期把區塊連同內容丟掉，連 `@media (min-width: 1px)` 的對照組都沒生存。所以退路不是換寫法。JS 側套件也沒有任何讀系統設定的 API。**連帶結論：任何 `@media` 的用法都不成立，包括響應式斷點**。`HANDOFF.md` §11 那條「且 `prefers-reduced-motion: reduce` 會全部關閉」的檢查項是設計稿的紀錄，保留但不成立 | §12.23（含兩條重新評估的觸發條件：CSS 管線開始保留 `@media`，或平台開放系統設定 API） |
| masthead 的四個規模計數（種類／形態／MEGA／招式） | 四個數字都是內建資料集的常數，不隨查詢、語系或任何操作改變，所以第二次看起來就沒有資訊，卻在每一個畫面上佔著 masthead 的高度 —— 而那段高度是有人在搶的：查詢列為了讓出它被壓縮過，`add-moves-tab` 的分頁鍵也需要一個常駐的位置。**另外它自交付以來其實一直沒被渲染**：提案交付了它（commit bc97f69），後來一次手動 UI 調整（commit c617a76）把 template 裡的元素刪掉，留下一個沒人讀的 computed 與四條死樣式規則，三道檢查全都看不到（樣式檢查讀樣式表不讀元素樹、`vue-tsc` 不為未使用的 computed 報錯、測試不建元素樹）。**四個數字本身沒有消失**，仍在資料集的 meta block 裡並且仍被 `dex-data` 的載入期不變式斷言。⚠️ **`i18n.ts` 少了 `tSpecies`／`tForms`／`tMega`／`tMoves` 這四個鍵是這個決定的結果，不是功能漏失** —— 本檔「維護方式」下方那條「字串表少了某個鍵是該功能沒被做的最快指標」在這一項上不適用 | `add-moves-tab` 的 design.md「移除 masthead 的四個規模計數」、`dataset-statements` spec 的 REMOVED Requirement（含 Reason 與 Migration） |
| 氛圍層（ordered dither 暈影） | 純裝飾層，不承載資訊。Lynx 無 canvas，替代路徑要嘛引入專案的第一個建置期資產生成步驟（tile 顏色隨模式，需每模式一組共四張），要嘛以數百個 view 疊格子（§12.14 已記 208 張卡的首次繪製就比直覺慢）。代價與收益不成比例。**曾是文件狀態最糟的一項** —— spec 預告過它，那半句已移除。§9 四條決策與 §11 三條檢查項原文保留，是設計稿的紀錄 | §12.20（含三條重新評估的觸發條件） |

### 曾列在 C 節、已被推翻

一項移出 C 節不代表它悄悄消失了。記在這裡，因為「為什麼當初不做」與「為什麼後來做了」對下一個提案同樣有用。

| 項目 | 當初的理由 | 為什麼翻案 |
|---|---|---|
| 招式表自帶捲動區（設計稿 `max-height: 400px`） | 巢狀同向捲動搶同一個手勢，`species-detail` spec 立下「面板恰好一個捲動容器」（§12.16 原本把這條禁令記成 `dex-grid` 的，是錯的 —— 那條說的是網格不得嵌在別的捲動容器內） | 代價付了一批之後判斷不值得。已實作（`bound-learnset-scroll`）：高度 `36vh` 而非 400px、只在顯示列數 > 12 時才套、表頭移到捲動容器外。spec 的禁令改為「限於面板本體與招式表」，例外只給這一層。**iOS 實機已結清：手勢依落點分派，兩層不搶** —— 當初的顧慮在本平台不成立，見 §12.19 |

---

## 維護方式

- 這份檔案的權威範圍就是上面 A／B／C 三節。**動了 `src/` 之後如果差距變了，改這裡** ——
  不要只改 `HANDOFF.md`，那份文件是設計稿的交接，不是移植版的待辦
- 一項做完就從 A 移走，把它的行為寫進對應的 `openspec/specs/` —— **spec 才是完成的定義**，
  這裡只記「還沒被要求過的東西」
- 一項決定不做就移到 C 節，並寫下理由的位置。C 節存在的目的是讓同一個提議不被提第二次
- **編號不重編。** 一項移走之後，其餘各項保留原編號（所以 A 節會有缺號）—— change 的 proposal
  與 design 會直接引用「ROADMAP A7」這種編號，重編會讓那些引用靜默指向別的項目
- C 節的一項後來被推翻，就移到「曾列在 C 節、已被推翻」並同時記下兩邊的理由。**不要靜默刪除**
  —— 那會讓下一個人重新發現同一件事，卻不知道它已經被權衡過兩次
- 每次重新對照設計稿後更新開頭的「最後對照」行與 commit
