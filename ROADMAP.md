# ROADMAP — 移植版與設計稿的差距

第一階段（五個 change）交付的是**設計稿的主要功能**，不是設計稿的全部。這份檔案是那個差額的
單一清單，讓後續工作不必每次重讀 `design/HANDOFF.md` 才知道還缺什麼。

第六個 change（`bound-learnset-scroll`）已 archive —— 它不是補這份清單裡的缺口，而是推翻了
C 節的一項決定，見「曾列在 C 節、已被推翻」。

**這份檔案管什麼**：設計稿有、移植版沒有的東西，以及移植版整體缺的非功能能力。
**這份檔案不管什麼**：平台事實（`design/HANDOFF.md` §12）、資料來源與推導規則（同檔 §5–§7）、
已完成功能的行為（`openspec/specs/`）。三者都還是各自的權威，這裡只在需要時指路。

最後對照：2026-07-30，對照的是 `bound-learnset-scroll` archive 之後的樹（前一次對照是 `afc4d05`）。
A 節十項與 B 節六項**都還沒有動過**，本次只有 C 節的一項被推翻。

---

## 為什麼這些項目會漏掉

值得先講，因為它決定了怎麼避免下一批再漏。

移植版的 spec 是**逐批**寫的，每一批只描述那一批要交付的東西。設計稿裡沒被排進任何一批的
東西，因此既不在 spec 裡（所以 `spectra` 的檢查看不到），也不在 §12.16 的偏離清單裡（那份清單
只涵蓋詳情面板與招式表），也不在 `pnpm run check` 裡。**缺的東西從來沒有被要求過。**

這與 §12.18 記的失效是同一個機制的另一半：那次是字串跑在實作前面（佔位字承諾了未實作的搜尋
範圍），這次是字串跟著實作一起消失 —— `i18n.ts` 少了 `footer`、`megaOnly`、`multiOnly`、
`sortName`、`count` 與四個 tally 鍵，於是畫面上沒有任何東西指出功能不見了。**§12.18 只寫了
前一半。**

---

## A. 功能差距

三項排在前面，因為它們各自有「文件或 spec 已經預告、但沒有交付」的性質 —— 那比單純的少做一個
功能更貴，會讓後人相信它已經存在。

### A1. 氛圍層（ordered dither 暈影）——⚠️ spec 已預告，未交付

| | |
|---|---|
| 設計稿 | `design/pipeline/template.html:41` `body::before`，canvas 生成 4×4 tile，密度往外緣遞增 2/16 → 8/16 |
| 移植版 | 零實作 |
| 為什麼算漏記 | `openspec/specs/retro-theme/spec.md:95` 寫著四階灰 ramp「is the colour source for later sprite placeholders and **the ambience layer**」—— spec 把它當成之後會來的東西。`HANDOFF.md` §3 列為第七項功能、§9 有**四條**設計決策（為何用抖動而非漸層、為何只在外框之外、為何用 canvas 生成、為何斜角用現有色階）、§11 有**三條**檢查項。全部懸空 |
| 平台障礙 | Lynx 元素清單沒有 canvas（§10 已記）。這是真的障礙，但從未被寫成一個決定 |
| 要做的話 | 建置期產出 4×4 PNG tile（兩種密度各一張、顏色取自當前模式）用 `<image>` 平鋪；或以 view 疊格子。前者的顏色要隨模式切換，所以 tile 得是每模式一組 |
| 不做的話 | 也要落地：在 §12 寫一條「已評估放棄」並**同步修掉 spec 第 95 行那句預告**。現在是最糟的狀態 —— spec 說它會來，沒人記得它沒來 |

### A2. footer 六段來源與版權說明 —— ⚠️ 對外聲明，未交付

| | |
|---|---|
| 設計稿 | i18n 表的 `footer` 鍵，六段：陣容與形態／招式／種族值與特性／中文名稱／圖像／字型與版權 |
| 移植版 | `i18n.ts` 沒有 `footer` 鍵，`App.vue` 沒有對應區塊 |
| 為什麼要補 | 這是畫面上**唯一**聲明資料來源、非商業用途與 © 的地方。`README.md` 有，但那不是使用者會看到的東西。另外 `HANDOFF.md` §8 那條「第六世代之後是渲染圖，**已在 footer 誠實說明**」在移植版並不成立 |
| 成本 | 最低的一項 —— 內容原文都在 `template.html` 的 I18N 表裡，搬字串加一個 `<scroll-view>` 內的區塊即可。散文面已內嵌，字型分工照 §9（標籤 Silkscreen、內文 Literata） |
| ⚠️ 注意 | 六段裡有 `__ROSTER__` 佔位字，是 `build.py` 在建置期填入的來源 URL。移植版沒有那個替換步驟，要嘛寫死、要嘛從 `dex.json` 帶過來 |

### A3. `prefers-reduced-motion` —— ⚠️ 檢查項還留著，平台未量

| | |
|---|---|
| 設計稿 | `template.html:173`，把 `veilIn`／`panelIn`／`.big`／卡片揭示四個動畫全部關成 `animation: none` |
| 移植版 | `App.css` 有三個 `@keyframes`（267 `cardIn`、617 `veilIn`、626 `panelIn`），零 `@media` |
| 為什麼算漏記 | `HANDOFF.md` §11 的檢查項原文照抄著「且 `prefers-reduced-motion: reduce` 會全部關閉」，而 spec 沒有任何一條要求它，實作也沒有 |
| 未知 | **§12 從未量過 Lynx 是否支援這個 media query。** 這是設計稿的宣告裡唯一還沒被問過的平台問題 —— 其他都問過了 |
| 要做的話 | 先量。失敗模式正是 §12.17 記的第三類坑（宣告寫得下去、不報錯、行為沒發生），所以判別方式要是「無條件、無限循環的探針」而不是看樣式表。若不支援，退路是讀系統設定的 JS API（若有）或承認做不到並寫進 §12 |

### A4. 型別與世代篩選從「可多選」縮為「單選」

| | |
|---|---|
| 設計稿 | `state.types` 與 `state.gens` 都是 `Set`，可同時選多個 |
| 移植版 | `src/state/query.ts` 是 `TypeName \| null` 與 `number \| null` |
| 為什麼算漏記 | `dex-query` spec 通篇用單數（「the type filter」），所以不違規 —— 但這是**行為縮減**，沒有任何一處寫它是刻意的 |
| 要做的話 | 多選會帶出一個設計稿沒答的問題：多個型別是 AND 還是 OR。設計稿的 `match()` 是 OR（任一命中）。改動範圍是 `query.ts` 兩個 ref 與 `QueryBar.vue` 的選中判斷，加上 `dex-query` spec 的兩條需求 |

### A5. `★ 僅 MEGA` 與 `僅多形態` 兩顆篩選鈕

設計稿 `state.mega` / `state.multi`，移植版零出現（連 i18n 鍵都沒有）。純加法，
`query.ts` 兩個布林 + `QueryBar.vue` 兩顆鈕 + spec 一條需求。

### A6. 名稱排序

設計稿三種排序（編號／種族值／名稱），移植版兩種。名稱排序用 `localeCompare` 並依當前語系
選 `zh-Hant` 或 `en` —— **這一項要注意平台**：Lynx 的 JS 引擎是否帶完整 ICU 沒有量過，
`localeCompare` 可能退化成 code-point 比較（中文會排成筆畫無關的順序）。

`dex-query` spec 用「closed set containing **at least** national number and base-stat total」
把兩種排序合法化了，補第三種不違反既有 spec。

### A7. masthead 的 tally 四個計數

| | |
|---|---|
| 設計稿 | 種類／形態／MEGA／招式四個計數（208 / 360 / 75 / 496） |
| 移植版 | `App.vue:19` 只有 `{{ results.length }} / 208` |
| 順帶兩個小缺陷 | ①`208` 是**寫死的字面值**，不是 `dex.species.length` —— 而資料層的不變式正好斷言這個數字，寫死等於把它抄了第二份 ②設計稿的 `count(n,tot)` 是本地化字串（「n / 208 種類」），移植版是裸斜線，`i18n.ts` 沒有 `count` 鍵 |

### A8. 種族值排序時卡片不顯示 BST 數字

設計稿在 `state.sort === 'bst'` 時把 `bestBst(sp)` 印在卡片型別列右側（`.bs`），移植版沒有。
`bestBst` 已存在於 `src/data/dex.ts`，只在 `query.ts` 的排序裡用到。少了它，種族值排序的結果
無法在畫面上驗證 —— 使用者看到順序卻看不到依據。

### A9. 查詢列的世代鈕是阿拉伯數字

`QueryBar.vue:109` 印 `{{ gen }}`，設計稿是 `GEN_ROMAN[i]`（I–IX）。**卡片與詳情面板是對的**
（`genNumeral` / `genOfLabel` 都走 `GEN_ROMAN`），所以這是同一份資料在三處顯示、其中一處不
一致。§9 的年代語彙論述適用。一行的改動。

### A10. 離線探測與整批佔位

設計稿開機時探測一次遠端圖庫（`probe()` / `REMOTE`），失敗就**整批**改用繪製的佔位圖，理由
寫在原始碼註解裡：避免數百張破圖閃過。移植版是逐張 `@load`，離線時每張卡各自等自己的 timeout。

§12.16 只記了移除 `new Image()` 預熱，沒記整批探測這件事。要不要補取決於離線是否算目標情境 ——
若不算，把這個決定寫下來就夠了。

---

## B. 非功能差距

這些不是「設計稿有而移植沒有」，而是移植版整體缺的能力。`HANDOFF.md` 沒有涵蓋任何一項。

| 項目 | 現況 | 影響 |
|---|---|---|
| **自動化測試** | 只有 `scripts/check-styles.mjs`、`scripts/check-contrast.mjs` 與 `tsc` | spec 裡上百條 Scenario 與 Example（`dragon` 19 筆、`gen5` 29 筆、六欄左緣 195/223/267/311…）**沒有一條是機器驗的**，全是文件裡的數字。資料層的六項不變式是載入期斷言，沒有 test runner |
| **CI** | 沒有 `.github/` | 四項 check 靠人記得跑 |
| **無障礙** | 移植版零 a11y 屬性 | 設計稿有 `aria-pressed`（模式／語系／世代／型別／排序鈕）、`aria-current`（選中的卡片）、`role="dialog"` + `aria-modal`、`label for`。Lynx 的 accessibility 屬性**沒查過**。這個主題 HANDOFF 全文未提 |
| **效能與記憶體數字** | 只有定性結論 | §12.13／§12.17 的「無卡頓」沒有任何數字，而 §12.14 自己說「208 張卡的首次繪製比直覺慢得多」。真的要優化時沒有基線 |
| **Android** | 兩筆掛帳（§10：字符渲染、字型註冊） | §12.17 末尾說「依專案當前決定不在範圍內」，§10 那張「開工前必須確認的兩件事」的表卻還開著第 2 筆。**兩處敘述矛盾，需要挑一個** |
| **驗收截圖** | `shots/` 停在 13 張 | §12.17 兩項與 §12.19 三項 iOS 結清項目都沒有截圖（兩節都自己承認）。重跑回歸的條件各寫在該節末尾。**手勢類的驗收本來就拍不出來** —— §12.19 那三項是「哪一層吃到手勢」，靜態截圖無法呈現，要留證據得錄影 |

---

## C. 已確認不做 —— 不要當成待辦重新提出

每一項都有平台或設計理由，理由在指定的位置。**在改回設計稿的寫法之前先讀它。**

| 設計稿的東西 | 為什麼不做 | 理由在 |
|---|---|---|
| 四處 `title` hover 提示（型別中文名、形態鈕型別、招式另一語言名、傷害類別全名） | 觸控裝置沒有 hover。前兩者的資訊已直接寫進藥丸與鈕，後兩者不需要替代物 | §12.16、§8 末列 |
| 左右方向鍵切換形態、Escape 關閉面板 | 裝置沒有鍵盤（§3 的功能敘述尚未更新，是文件債不是功能債） | 本檔 |
| `<list>` 承載網格或招式表 | vue-lynx 只實作尾端追加，篩選與排序在它上面不成立。**這是「當時不能用」不是「本專案偏好 scroll-view」** | §12.13（含重新評估的觸發條件） |
| `font-variant-numeric: tabular-nums` | 平台的字型註冊不吃 font-variant 描述子，且像素字型的數字本來就不等寬。**樣式表不得出現這個屬性** | §12.17 |
| `new Image()` 預熱其他形態的圖 | 平台沒有 `Image` 建構子 | §12.16 |
| 三處 `display: grid` | `App.css` 零處 grid，flex + 固定欄寬對齊結果相同 | §12.16 |
| 半透明遮罩以外的任何半透明表面 | 會合成出四階灰以外的顏色。遮罩是唯一例外，**不是前例** | §12.16、`retro-theme` spec |
| 存取捲動位置的程式 | Vue 的響應式更新本來就留著捲動位置。**不要「補」上去** | §12.16 |
| 面板**其他區段**自帶捲動區 | `species-detail` spec 把例外限給招式表，並明文寫「不是前例」。⚠️ 這條的理由已經換過：§12.19 實測巢狀同向捲動**技術上成立**，所以「辦不到」不再是理由，剩下的是「沒有一段長到需要」—— 種族值六列、特性最多三塊、大圖固定 192px。**拿 §12.19 當通行證在其他區段加捲動區是誤讀** | §12.19、`species-detail` spec |
| 搜尋語料收錄裸羅馬數字世代 token | 搜 `V` 會命中 208 筆裡的 125 筆，與「搜尋壞了」無法區分 | §12.18 |

### 曾列在 C 節、已被推翻

一項移出 C 節不代表它悄悄消失了。記在這裡，因為「為什麼當初不做」與「為什麼後來做了」對下一個提案同樣有用。

| 項目 | 當初的理由 | 為什麼翻案 |
|---|---|---|
| 招式表自帶捲動區（設計稿 `max-height: 400px`） | 巢狀同向捲動搶同一個手勢，`species-detail` spec 立下「面板恰好一個捲動容器」（§12.16 原本把這條禁令記成 `dex-grid` 的，是錯的 —— 那條說的是網格不得嵌在別的捲動容器內） | 代價付了一批之後判斷不值得。已實作（`bound-learnset-scroll`）：高度 `36vh` 而非 400px、只在顯示列數 > 12 時才套、表頭移到捲動容器外。spec 的禁令改為「限於面板本體與招式表」，例外只給這一層。**iOS 實機已結清：手勢依落點分派，兩層不搶** —— 當初的顧慮在本平台不成立，見 §12.19 |

---

## 維護方式

- 這份檔案的權威範圍就是上面 A／B／C 三節。**動了 `src/` 之後如果差距變了，改這裡** ——
  不要只改 `HANDOFF.md`，那份文件是設計稿的交接，不是移植版的待辦
- 一項做完就從 A 移走，把它的行為寫進對應的 `openspec/specs/` —— **spec 才是完成的定義**，
  這裡只記「還沒被要求過的東西」
- 一項決定不做就移到 C 節，並寫下理由的位置。C 節存在的目的是讓同一個提議不被提第二次
- C 節的一項後來被推翻，就移到「曾列在 C 節、已被推翻」並同時記下兩邊的理由。**不要靜默刪除**
  —— 那會讓下一個人重新發現同一件事，卻不知道它已經被權衡過兩次
- 每次重新對照設計稿後更新開頭的「最後對照」行與 commit
